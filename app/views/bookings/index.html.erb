<div class="container mt-5 text-center">
  <%= link_to new_booking_path, class: "home-link" do %>
    <button class="home_button reservation_button">
      <div class="grey">
        ΚΑΝΕ ΜΙΑ ΚΡΑΤΗΣΗ
      </div>
    </button>
  <% end %>
  <%= week_calendar(events: @bookings) do |date| %>
    <div class="day">
      <%= date.day %>
    </div>
  <% end %>
</div>
